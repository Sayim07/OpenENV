{
  "name": "meta-ai-hackathon",
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run dev --prefix dashboard",
    "backend": "set PYTHONPATH=email_triage_env&& uvicorn email_triage_env.app:app --host 0.0.0.0 --port 7860 --reload",
    "test:env": "pytest email_triage_env/tests",
    "baseline": "python email_triage_env/baseline.py",
    "docker:up": "docker-compose up --build"
  }
}
